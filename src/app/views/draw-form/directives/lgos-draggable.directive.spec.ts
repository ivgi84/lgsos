import { LgosDraggableDirective } from './lgos-draggable.directive';

describe('LgosDraggableDirective', () => {
  it('should create an instance', () => {
    const directive = new LgosDraggableDirective();
    expect(directive).toBeTruthy();
  });
});
